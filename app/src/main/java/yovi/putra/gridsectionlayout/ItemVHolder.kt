package yovi.putra.gridsectionlayout

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class ItemVHolder(itemView: View) : BaseVHolder<Item>(itemView) {
    override fun binding(data: Item) {
    }
}